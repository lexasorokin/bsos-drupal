id: custommig
label: Dept Migrations
decription: Drupal 7 custom migration group for depts
source_type: Drupal 7
shared_configuration:
  source:
    key: migrate
#above key is the name in settings.php for the D7 database settings
dependencies:
  enforced:
    module:
      - migrate_customdept
